# RhythmAI — Frontend Environment Variables
VITE_API_URL=http://localhost:8001

# AI Clinical Assistant is powered by the backend and works locally by default.
# Optional remote Hugging Face settings can be configured in the backend if needed.
