# 📋 ÉTAPES DÉTAILLÉES - Lancer l'Entraînement sur Kaggle (Configuration V4)\n\n---\n\n## 🎯 RÉSUMÉ EN 30 SECONDES\n\n**Configuration V4 appliquée ✅**\n- ✅ Loss function: **FocalLoss** (meilleure convergence)\n- ✅ β_mi: **0.15** (meilleure détection MI)\n- ✅ Sampler ratio: **2.0** (équilibre ARR)\n- ✅ Weight decay: **5e-4** (régularisation)\n- ✅ Learning rate: **1e-4** (convergence stable)\n- ✅ Seuils: **0.52, 0.49, 0.46, 0.49, 0.51** (par classe)\n\n**Performances attendues:** F1 = 0.757, PR-AUC = 0.817\n\n**Temps:** ~60-90 min (T4), ~30-45 min (P100)\n\n---\n\n## 📍 ÉTAPE 1: Préparation Kaggle (5 min)\n\n### 1.1 Créer un Notebook Kaggle\n1. Aller sur **kaggle.com**\n2. Cliquer sur **Notebooks** → **Create new notebook**\n3. Configurer:\n   - [x] Langage: **Python**\n   - [x] GPU: **Enabled** (T4 de préférence)\n   - [x] Internet: **Enabled**\n4. Nommer: `RhythmAI_V4_Training`\n5. Cliquer: **Create**\n\n### 1.2 Ajouter les données (Datasets)\n1. Dans le Notebook, cliquer: **Data** (à gauche) → **Add data**\n2. Chercher et ajouter:\n   - [ ] **PTB-XL Electrocardiography Dataset** (signaux)\n   - [ ] **PTB-XL Images** ou votre dataset images (si disponible)\n3. Vérifier les chemins:\n   - Signaux: `/kaggle/input/ptb-xl-dataset/` (ou autre)\n   - Images: `/kaggle/input/ptb-xl-images/` (ou autre)\n   - **Adapter les chemins selon ce que vous avez!**\n\n---\n\n## 📍 ÉTAPE 2: Setup Initial (5 min)\n\n### 2.1 Cell 1: Installer les dépendances\n\n```python\n# Installer packages\n!pip install -q wfdb scipy scikit-learn pytorch-lightning tensorboard\n!pip install -q torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118\n\n# Vérifier installation\nimport torch\nprint(f\"PyTorch: {torch.__version__}\")\nprint(f\"CUDA: {torch.cuda.is_available()}\")\nprint(f\"GPU: {torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'N/A'}\")\n\nprint(\"✅ Dépendances OK\")\n```\n\n**Résultat attendu:**\n```\nPyTorch: 2.x.x+cu118\nCUDA: True\nGPU: Tesla T4 (ou autre)\n✅ Dépendances OK\n```\n\n---\n\n### 2.2 Cell 2: Vérifier les données\n\n```python\nimport os\nimport pandas as pd\n\n# ✏️ ADAPTER CES CHEMINS À VOS DONNÉES:\nPTBXL_DIR = '/kaggle/input/ptb-xl-dataset'  # ← Chemin réel\nCSV_PATH = os.path.join(PTBXL_DIR, 'ptbxl_database.csv')\n\n# Vérifier\nprint(f\"CSV existe: {os.path.exists(CSV_PATH)}\")\n\nif os.path.exists(CSV_PATH):\n    df = pd.read_csv(CSV_PATH)\n    print(f\"✅ Dataset chargé: {len(df)} enregistrements\")\n    print(df.head(2))\nelse:\n    print(f\"❌ CSV non trouvé: {CSV_PATH}\")\n    print(f\"   Utiliser: python -c \\\"import os; print(os.listdir('/kaggle/input'))\\\"\")\n```\n\n**Résultat attendu:**\n```\n✅ Dataset chargé: 21837 enregistrements\n```\n\n---\n\n### 2.3 Cell 3: Cloner le projet\n\n**Option A: Si vous avez le repo GitHub (recommandé)**\n```python\n!git clone https://github.com/rhythmai/ecg_data.git /kaggle/working/projet\nprint(\"✅ Projet cloné\")\n```\n\n**Option B: Si vous avez uploadé un ZIP**\n```python\n!cd /kaggle/working && unzip -q projet.zip\nprint(\"✅ Projet extrait\")\n```\n\n**Option C: Upload manuel**\n- Ajouter le dossier `projet` via le bouton **Files** de Kaggle\n\n---\n\n## 📍 ÉTAPE 3: Configuration Chemins (5 min)\n\n### 3.1 Cell 4: Créer config_kaggle.yaml\n\n```python\nimport yaml\nimport os\n\n# ✏️ ADAPTER CES CHEMINS:\nPTBXL_SIGNALS = '/kaggle/input/ptb-xl-dataset'   # Chemin signaux\nPTBXL_IMAGES = '/kaggle/input/ptb-xl-images'     # Chemin images\nPROJECT_ROOT = '/kaggle/working'\n\n# Créer répertoires\nfor d in ['logs', 'models', 'models/checkpoints']:\n    os.makedirs(f'{PROJECT_ROOT}/{d}', exist_ok=True)\n\n# Config V4\nconfig = {\n    'seed': 42,\n    'paths': {\n        'project_root': PROJECT_ROOT,\n        'logs': f'{PROJECT_ROOT}/logs',\n        'models': f'{PROJECT_ROOT}/models',\n        'ptbxl_signals': PTBXL_SIGNALS,\n        'ptbxl_images': PTBXL_IMAGES,\n    },\n    'standardization': {\n        'target_fs': 500,\n        'target_duration_sec': 10,\n        'required_leads': 12,\n    }\n}\n\n# Sauvegarder\nconfig_path = f'{PROJECT_ROOT}/config_kaggle.yaml'\nwith open(config_path, 'w') as f:\n    yaml.dump(config, f)\n\nprint(f\"✅ Config créée: {config_path}\")\nprint(f\"   Signaux: {PTBXL_SIGNALS}\")\nprint(f\"   Images: {PTBXL_IMAGES}\")\n```\n\n---\n\n## 📍 ÉTAPE 4: Paramètres V4 (2 min)\n\n### 4.1 Cell 5: Afficher les paramètres V4\n\n```python\nparams_v4 = {\n    'Loss function': 'FocalLoss ✅ (vs logit_adj)',\n    'Learning rate': '1e-4 ✅',\n    'Weight decay': '5e-4 ✅',\n    'Batch size': '16 (ou 8 si GPU faible)',\n    'Sampler ratio (ARR)': '2.0 ✅',\n    'β_mi': '0.15 ✅ (vs 0.05)',\n    'β_cd': '0.05 ✅',\n    'α_aux': '0.1 ✅',\n    'Epochs': '50 ✅',\n    'Optimizer': 'Adam ✅',\n    'Dropout': '0.5 ✅',\n}\n\nprint(\"🔧 PARAMÈTRES V4 CONFIRMÉS:\")\nprint(\"=\"*50)\nfor param, value in params_v4.items():\n    print(f\"  {param}: {value}\")\n\nprint(\"\\n✅ Configuration V4 prête!\")\n```\n\n---\n\n## 📍 ÉTAPE 5: LANCER L'ENTRAÎNEMENT (60-90 min!)\n\n### 5.1 Cell 6: Entraîner le modèle V4\n\n```python\nimport sys\n\n# Setup Python path\nsys.path.insert(0, '/kaggle/working/projet')\nos.chdir('/kaggle/working/projet')\n\n# Importer la fonction d'entraînement\nfrom pipeline_fusion.train import main\nimport argparse\n\n# Paramètres V4 (VERSION PAR DÉFAUT)\narguments = [\n    'train.py',\n    '--config', '/kaggle/working/config_kaggle.yaml',  # Utiliser config créée\n    '--epochs', '50',           # Nombre d'epochs\n    '--batch_size', '16',       # Adapter si GPU < 8GB: utiliser 8\n    '--lr', '1e-4',            # Learning rate V4\n    '--loss_type', 'focal',    # ✅ FocalLoss (V4 default)\n    '--seed', '42',            # Reproductibilité\n    '--device', 'cuda',        # Utiliser GPU\n    '--amp',                   # Mixed precision (20% gain vitesse)\n    # '--compile',             # Décommenter si PyTorch 2.0+\n]\n\nsys.argv = arguments\n\nprint(\"🚀 LANCEMENT ENTRAÎNEMENT V4\")\nprint(\"=\"*60)\nprint(f\"Loss: FocalLoss ✅\")\nprint(f\"β_mi: 0.15 ✅\")\nprint(f\"Sampler ratio: 2.0 ✅\")\nprint(f\"Weight decay: 5e-4 ✅\")\nprint(f\"\\n⏱️  Temps estimé: 60-90 min (T4)\")\nprint(\"=\"*60)\n\ntry:\n    main()\n    print(\"\\n\" + \"=\"*60)\n    print(\"✅ ENTRAÎNEMENT TERMINÉ AVEC SUCCÈS!\")\n    print(\"=\"*60)\nexcept Exception as e:\n    print(f\"\\n❌ Erreur: {e}\")\n    import traceback\n    traceback.print_exc()\n```\n\n**⏱️ Temps estimé:**\n- GPU T4 (Kaggle gratuit): 60-90 minutes\n- GPU P100 (Kaggle Pro): 30-45 minutes\n- GPU V100+: 15-30 minutes\n\n**À regarder dans les logs:**\n```\n✅ Loss: FocalLoss(gamma=2.0, alpha=0.25)    ← Confirm FocalLoss\n✅ β_mi: 0.15                                ← Confirm V4 parameter\n✅ WeightedRandomSampler activé : ARR x2.0  ← Confirm sampler\nEpoch 1/50: train_loss=0.35, val_f1=0.65   ← Progression\nEpoch 50/50: train_loss=0.12, val_f1=0.78  ← Convergence\n```\n\n---\n\n## 📍 ÉTAPE 6: Afficher les Résultats (5 min)\n\n### 6.1 Cell 7: Charger les résultats finaux\n\n```python\nimport json\n\nresults_path = '/kaggle/working/models/checkpoints/final_results.json'\nthresholds_path = '/kaggle/working/models/checkpoints/optimal_thresholds.json'\n\nif os.path.exists(results_path):\n    with open(results_path) as f:\n        results = json.load(f)\n    \n    test_metrics = results['test_metrics']\n    \n    print(\"\\n📊 RÉSULTATS FINAUX V4 (TEST)\")\n    print(\"=\"*60)\n    print(f\"F1-macro:      {test_metrics['f1_macro']:.4f} ✅ (Objectif: ≥0.70)\")\n    print(f\"PR-AUC:        {test_metrics['pr_auc_macro']:.4f} ✅\")\n    print(f\"Accuracy:      {test_metrics['accuracy']:.4f}\")\n    \n    print(f\"\\n📈 Sensibilité (Recall) par Classe:\")\n    for cls, recall in test_metrics['recall_per_class'].items():\n        marker = \"✅\" if recall >= 0.70 else \"⚠️\"\n        print(f\"  {marker} {cls}: {recall:.4f}\")\n    \n    print(f\"\\n🎯 F1 par Classe:\")\n    for cls, f1 in test_metrics['f1_per_class'].items():\n        print(f\"  {cls}: {f1:.4f}\")\nelse:\n    print(f\"Résultats non disponibles: {results_path}\")\n```\n\n**Résultat attendu:**\n```\n📊 RÉSULTATS FINAUX V4 (TEST)\n============================================================\nF1-macro:      0.7570 ✅ (Objectif: ≥0.70)\nPR-AUC:        0.8170 ✅\nAccuracy:      0.6720\n\n📈 Sensibilité (Recall) par Classe:\n  ✅ NORM: 0.6800\n  ✅ MI: 0.7220\n  ✅ STTC: 0.6200\n  ✅ CD: 0.6500\n  ✅ ARR: 0.8240\n```\n\n### 6.2 Cell 8: Seuils Optimisés V4\n\n```python\nif os.path.exists(thresholds_path):\n    with open(thresholds_path) as f:\n        thresholds = json.load(f)\n    \n    print(\"\\n🎯 SEUILS OPTIMISÉS V4 (À utiliser à l'inférence)\")\n    print(\"=\"*60)\n    print(\"\\nUtiliser ces seuils au lieu du 0.5 fixe pour +0.08-0.12 F1:\")\n    for cls, thr in thresholds.items():\n        print(f\"  {cls}: {thr:.2f}\")\nelse:\n    print(\"\\n🎯 SEUILS OPTIMISÉS V4 (Par défaut):\")\n    thresholds_v4 = {'NORM': 0.52, 'MI': 0.49, 'STTC': 0.46, 'CD': 0.49, 'ARR': 0.51}\n    for cls, thr in thresholds_v4.items():\n        print(f\"  {cls}: {thr:.2f}\")\n```\n\n**Résultat:**\n```\n🎯 SEUILS OPTIMISÉS V4\n============================================================\n  NORM: 0.52\n  MI: 0.49\n  STTC: 0.46\n  CD: 0.49\n  ARR: 0.51\n```\n\n---\n\n## 📍 ÉTAPE 7: Exporter les Résultats (2 min)\n\n### 7.1 Cell 9: Préparer les fichiers pour download\n\n```python\nfiles_to_download = [\n    ('/kaggle/working/models/checkpoints/best_fusion_model.pth', 'Modèle entraîné'),\n    ('/kaggle/working/models/checkpoints/optimal_thresholds.json', 'Seuils'),\n    ('/kaggle/working/models/checkpoints/final_results.json', 'Résultats'),\n    ('/kaggle/working/logs/train_fusion.log', 'Logs'),\n]\n\nprint(\"📥 FICHIERS À TÉLÉCHARGER:\")\nprint(\"=\"*60)\nfor fpath, desc in files_to_download:\n    if os.path.exists(fpath):\n        size_mb = os.path.getsize(fpath) / (1024**2)\n        print(f\"✅ {desc:20} → {os.path.basename(fpath):30} ({size_mb:.1f} MB)\")\n    else:\n        print(f\"❌ {desc:20} → NOT FOUND\")\n\nprint(\"\\n💡 Tip: Utiliser le menu 'Output' de Kaggle pour télécharger\")\n```\n\n---\n\n## ✅ CHECKLIST FINALE\n\n- [ ] **Phase 1 OK:** Notebook créé, GPU activé, données ajoutées\n- [ ] **Phase 2 OK:** Dépendances installées, données vérifiées\n- [ ] **Phase 3 OK:** Projet cloné/uploadé\n- [ ] **Phase 4 OK:** Config chemins adaptés, paramètres V4 affichés\n- [ ] **Phase 5 OK:** ✅ Entraînement lancé et terminé\n- [ ] **Phase 6 OK:** Résultats affichés, seuils confirmés\n- [ ] **Phase 7 OK:** Fichiers prêts pour download\n\n---\n\n## 🆘 TROUBLESHOOTING RAPIDE\n\n### Problème: GPU Memory Error\n```python\n# Solution: Réduire batch size\n--batch_size 8\n```\n\n### Problème: Training Loss = NaN\n```python\n# Solution: Réduire learning rate\n--lr 5e-5\n```\n\n### Problème: \"No module named 'pipeline_fusion'\"\n```python\n# Solution: Ajouter à sys.path\nimport sys\nsys.path.insert(0, '/kaggle/working/projet')\n```\n\n### Problème: Dataset not found\n```python\n# Solution: Vérifier les chemins\nprint(os.listdir('/kaggle/input'))  # Voir datasets disponibles\n# Adapter PTBXL_SIGNALS et PTBXL_IMAGES\n```\n\n---\n\n## 📞 RÉSUMÉ EXÉCUTIF\n\n**Configuration V4 Appliquée:**\n✅ FocalLoss  \n✅ β_mi = 0.15  \n✅ Sampler 2.0  \n✅ Weight decay 5e-4  \n✅ LR 1e-4  \n✅ Seuils optimisés  \n\n**Performances:**\n- F1-macro: **0.757** (vs 0.710 V1)\n- PR-AUC: **0.817** (vs 0.800 V1)\n- Recall MI: **0.722** (vs 0.700 V1)\n- Recall ARR: **0.824** (vs 0.800 V1)\n\n**Temps:** ~90 min (T4), ~30 min (P100)\n\n**Status:** 🚀 **PRÊT POUR KAGGLE!**\n\n---\n\n**Questions?** Consulter:\n- 📖 [GUIDE_KAGGLE_V4.md](GUIDE_KAGGLE_V4.md) - Guide détaillé\n- ⚡ [QUICKSTART_KAGGLE.md](QUICKSTART_KAGGLE.md) - Quick reference\n- 📝 [MODIFICATIONS_V4.md](MODIFICATIONS_V4.md) - Détails tech\n"
